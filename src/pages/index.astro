---
import BlogLayout from '../layouts/BlogLayout.astro';
import { Image } from 'astro:assets';
import proustScreenshot from '../content/projects/proust-screenshot.png';
---

<BlogLayout title="Tobias Fu" description="Tobias Fu's personal website">
  <main class="landing">
    <h1 class="landing-name">Tobias Fu</h1>

    <p class="landing-intro">
      I build things with code — mostly iOS apps and AI tools, and I am a part-time farmer.
    </p>
    <p class="landing-intro">
      I am interested in science, languages, and phenomenology.
    </p>
    <p class="landing-intro">
      Currently in Hawaii.
    </p>

    <hr class="landing-rule" />

    <section class="landing-projects">
      <div class="project-card">
        <div class="project-header">
          <span class="project-name">ProustGPT</span>
          <a class="project-link" href="https://github.com/realtobyfu/proust-gpt" target="_blank" rel="noopener">GitHub &#8599;</a>
          <a class="project-link" href="https://realtobyfu.github.io/proust-gpt/architecture.html" target="_blank" rel="noopener">Architecture &#8599;</a>
        </div>
        <p class="project-desc">A RAG system for exploring Marcel Proust's complete works. You ask questions, it finds relevant passages and synthesizes answers. Built with LangChain, Pinecone, FastAPI.</p>
        <a href="https://proust-gpt.onrender.com/" target="_blank" rel="noopener">
          <Image src={proustScreenshot} alt="ProustGPT screenshot" class="project-screenshot" />
        </a>
      </div>

      <hr class="project-rule" />

      <div class="project-card">
        <div class="project-header">
          <a class="project-name" href="/projects/fieldnote">Fieldnote</a>
          <a class="project-link" href="https://apps.apple.com/us/app/fieldnote-plant-journal/id6757382315" target="_blank" rel="noopener">App Store &#8599;</a>
        </div>
        <p class="project-desc">An app for identifying and journaling plants. Uses on-device ML so it works offline.</p>
      </div>

      <hr class="project-rule" />

      <div class="project-card">
        <div class="project-header">
          <a class="project-name" href="/projects/write-it-down">Write It Down</a>
          <a class="project-link" href="https://apps.apple.com/us/app/write-it-down/id6742731898" target="_blank" rel="noopener">App Store &#8599;</a>
        </div>
        <p class="project-desc">A notes app with location awareness and sharing. My first "real" app — learned a lot shipping this one.</p>
      </div>

      <hr class="project-rule" />

      <div class="project-card">
        <div class="project-header">
          <span class="project-name">Karma Farm</span>
          <span class="project-status">In progress</span>
        </div>
        <p class="project-desc">Location-based social app. I built the whole thing — iOS frontend, backend, database.</p>
      </div>
    </section>

    <a class="projects-link" href="/projects">All projects &#8599;</a>

    <hr class="landing-rule" />

    <footer class="landing-footer">
      <p>Previously worked on research on LLM interpretability at Tufts, CS degree (2025). Say hello at <a href="mailto:3tobiasfu@gmail.com">3tobiasfu@gmail.com</a>.</p>
    </footer>
  </main>
</BlogLayout>

<style>
  .landing {
    max-width: 640px;
    margin: 0 auto;
    padding: clamp(3rem, 8vw, 6rem) var(--container-padding) var(--space-3xl);
  }

  .landing-name {
    font-family: var(--font-title);
    font-weight: 700;
    font-size: clamp(2.5rem, 6vw, 3.5rem);
    letter-spacing: -0.03em;
    line-height: 1;
    color: var(--color-text);
    margin-bottom: var(--space-xl);
  }

  .landing-intro {
    font-family: var(--font-body);
    font-size: clamp(1rem, 2.5vw, 1.125rem);
    line-height: 1.7;
    color: var(--color-text-body);
    margin-bottom: var(--space-md);
  }

  .landing-intro:last-of-type {
    margin-bottom: 0;
  }

  .landing-rule {
    border: none;
    height: 1px;
    background: var(--color-border);
    margin: var(--space-2xl) 0;
  }

  .landing-projects {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .project-card {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .project-header {
    display: flex;
    align-items: baseline;
    gap: var(--space-md);
  }

  .project-name {
    font-family: var(--font-sans);
    font-weight: 600;
    font-size: var(--text-base);
    color: var(--color-text);
    text-decoration: none;
  }

  a.project-name:hover {
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .project-link {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color 0.15s ease;
  }

  .project-link:hover {
    color: var(--color-text);
  }

  .project-status {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }

  .project-desc {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: 1.5;
  }

  .project-rule {
    border: none;
    height: 1px;
    background: var(--color-border);
    margin: var(--space-lg) 0;
  }

  .project-screenshot {
    width: 100%;
    max-height: 300px;
    object-fit: cover;
    object-position: top;
    margin-top: var(--space-md);
    border-radius: 6px;
  }


  .projects-link {
    display: inline-block;
    margin-top: var(--space-lg);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color 0.15s ease;
  }

  .projects-link:hover {
    color: var(--color-text);
  }

  .landing-footer {
    color: var(--color-text-secondary);
    font-family: var(--font-body);
    font-size: var(--text-sm);
    line-height: 1.6;
  }

  .landing-footer a {
    color: var(--color-text-secondary);
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .landing-footer a:hover {
    color: var(--color-text);
  }

  @media (max-width: 480px) {
    .landing {
      padding-top: clamp(2rem, 6vw, 3rem);
    }

    .landing-name {
      font-size: 2rem;
    }

    .project-header {
      flex-direction: column;
      gap: 0;
    }
  }
</style>
